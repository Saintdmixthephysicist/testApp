import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Dimensions {
  var height;
  static double screenHeight = Get.context!.height;
    static double screenHWidth = Get.context!.width;

  
    static double screenHeight20 = screenHeight / 31;
        static double screenHeight7= screenHeight / 31;

        static double screenHeight50 = screenHeight / 12.4;

        static double screenwIDTH350 = screenHWidth/1.3;

static double  screenHeight15 = screenHeight / 41;
}
