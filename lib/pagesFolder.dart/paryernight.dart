import 'package:flutter/material.dart';

class PrayerNight extends StatelessWidget {
  const PrayerNight({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      body: Center(child: Text("this is the payer at night widet"),),
    );
  }
}