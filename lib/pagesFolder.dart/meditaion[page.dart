import 'package:flutter/material.dart';

class MediatationPAGE extends StatelessWidget {
  const MediatationPAGE({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("this is the mediatation page"),),
    );
  }
}