import 'package:flutter/material.dart';

class PrayerEvening extends StatelessWidget {
  const PrayerEvening({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("this is nthe prayer of the evening page")),
    );
  }
}
