import 'package:flutter/material.dart';

class PrayerMorning extends StatelessWidget {
  const PrayerMorning({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Colors.transparent),
      body: Center(child: Text("THIS US THE PRAYER FOR THE MORNING PAGE"),)
      
    );
  }
}