import 'package:flutter/material.dart';

class Mass extends StatelessWidget {
  const Mass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is the mass page"),
      ),
    );
  }
}
