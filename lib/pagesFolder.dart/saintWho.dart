import 'package:flutter/material.dart';

class SaintWho extends StatelessWidget {
  const SaintWho({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("This i sthe saint who page ")),
      
    );
  }
}